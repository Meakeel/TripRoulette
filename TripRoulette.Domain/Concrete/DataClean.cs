using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;

namespace TripRoulette.Domain.Concrete
{
    public class DataClean
    {
        public static string CleanString(string cleanme)
        {
            //Clean the String up then return
            return cleanme;
        }
        public static int CleanInt(int cleanme)
        {
            //Clean the Int up then return
            return cleanme;
        }

        public static DateTime CleanDate(DateTime cleanme)
        {
            //Clean the Int up then return
            return cleanme;
        }
        public static decimal CleanDecimal(decimal cleanme)
        {
            //Clean the Int up then return
            return cleanme;
        }
    }
}